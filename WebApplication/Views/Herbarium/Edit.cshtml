@model Herbarium

@{
  ViewBag.Title = "Edit herbarium";
}

<form asp-route-id="@Model.HerbariumId"
      asp-route-page="@ViewBag.Page"
      asp-route-sort="@ViewBag.Sort"
      asp-route-ascending="@ViewBag.Ascending"
      method="post">
    <div asp-validation-summary="All"></div>

    <div class="form-group">
        <label asp-for="InventoryNumber"></label>
        <div><span asp-validation-for="InventoryNumber" class="text-danger"></span></div>
        <input asp-for="InventoryNumber" class="form-control" />
    </div>
    <div class="form-group">
        <label asp-for="YearOfCollection"></label>
        <div><span asp-validation-for="YearOfCollection" class="text-danger"></span></div>
        <input asp-for="YearOfCollection" class="form-control" />
    </div>
    <div class="text-center">
        <button class="btn btn-primary" type="submit">Edit</button>
        <a asp-action="Index"
           asp-route-page="@ViewBag.Page"
           asp-route-sort="@ViewBag.Sort"
           asp-route-ascending="@ViewBag.Ascending"
           class="btn btn-secondary">Back</a>
    </div>
</form>

@section scripts {
  <partial name="IncludeValidation" />
}